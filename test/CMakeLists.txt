# QLog Test CMakeLists.txt

# 启用测试
enable_testing()

# 单元测试目标（暂时为空，等待实现）
# add_executable(qlog_test_cpp
#     cpp/test_atomic.cpp
#     cpp/test_spin_lock.cpp
# )
#
# target_link_libraries(qlog_test_cpp PRIVATE qlog gtest gtest_main)
# add_test(NAME qlog_tests COMMAND qlog_test_cpp)

message(STATUS "QLog: Test configuration ready (tests to be added in M0)")
