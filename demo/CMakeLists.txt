# QLog Demo CMakeLists.txt

# 示例程序目标（暂时为空，等待实现）
# add_executable(qlog_demo_basic demo/basic_example.cpp)
# target_link_libraries(qlog_demo_basic PRIVATE qlog)

message(STATUS "QLog: Demo configuration ready (demos to be added in M8+)")
